%{
open Ast
%}
%start start
%token EOF
%token < double > NUMBER
%type < Program > start
%%

start:
    | statement EOF     { Program [ $1 ] }

statement:
    | expression    { ExpressionStmt $1 }

expression:
    | NUMBER        { NumberLiteral $1 }
